for (let i = 1; i < 20; i++) {
    if (i % 3 === 0) {
        console.log(`${i} -> frontend`)
    }
    if (i % 5 === 0) {
        console.log(`${i} -> frontend`)
    }
    if (i % 3 === 0) {
        console.log(`${i} -> frontend`)
    }
    if (i % 3 === 0) {
        console.log(`${i} -> frontend`)
    }

}